%%
%% mean_filter.m
%%
%% Convolutes an image with a mean kernel
%%
%% Inputs:
%%	in_img		- N x M x 3 matrix representing a color image
%%	window_size	- The window size of mean kernel
%%
%% Outputs:
%%	out_img		- N x M x 3 matrix representing the blurred output
%%			  image
function out_img = mean_filter(in_img,window_size)

  %%
  %% Allocate the output image
  %% 

  out_img = zeros(size(in_img,1),size(in_img,2));

  %%
  %% If the window size is not odd, make it so
  %%

  window_size = window_size + mod(window_size,2);
  
  %%
  %% Compute the weight of each element in the window
  %%

  window_weight = 1/(window_size^2);

  %%
  %% Find the "Center" of the window
  %% 

  
  
  %%
  %% Loop through the image 
  %%


		%%
		%% Loop through the mean filter
		%%


	    %%
	    %% Check the boundaries
	    %%
	
